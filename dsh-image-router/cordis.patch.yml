- insert:
    - id: image-router
      name: 'dsh-image-router'
      # 默认配置即可用（DeepSeek 官方模型），如需改用其他/自定义供应商的模型：
      # config:
      #   enabled: true
      #   provider: deepseek-official
      #   model: deepseek-v4-flash
      #   maxTokens: 4096
      #   unknownCapability: pass   # 会话首条消息模型能力未知时：pass 放行 / route 路由
